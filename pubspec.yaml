name: bookley_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  dartz: ^0.10.1
  dio: ^5.1.2
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.4.0
  go_router: ^7.0.0
  google_fonts: ^4.0.4

dev_dependencies:
  flutter_test: 
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
